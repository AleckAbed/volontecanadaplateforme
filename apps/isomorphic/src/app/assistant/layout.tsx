import { ReactNode } from 'react';

export const metadata = {
  title: 'Volo — Assistant IA · Volonté Canada',
};

export default function AssistantLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
